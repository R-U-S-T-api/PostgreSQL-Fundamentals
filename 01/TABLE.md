

```sql

CREATE TABLE film (
 title VARCHAR(100),
 release_date DATE,
 age_restriction INT,
 rating REAL,
 country VARCHAR(60)
);

CREATE TABLE country (
	name VARCHAR(60),
	currency CHAR(3),
	population BIGINT
);


```